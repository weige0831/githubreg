# 本地配置与密钥（不要提交）
.zcode/
*.local.js
github-token.txt
.env

# 临时文件
.tmp-*
tmp/
node_modules/

# 测试跑出来的现场（截图 / 页面 HTML / 面板日志 —— 日志里可能带凭据），只上传到 Actions artifacts
artifacts/

# 编辑器
.vscode/
.idea/

# 本机工具脚本（带本机目录结构，不进公开仓库）
push-to-github.mjs
